filter:
  dependency_paths:
    - "vendor/"

build:
  environment:
    mysql: 5.7
  nodes:
    analysis:
      tests:
        override:
          - command: './bin/phing unit'
            coverage:
              file:   'test/coverage.xml'
              format: 'php-clover'
